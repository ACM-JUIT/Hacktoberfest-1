#include<stdio.h>

int main()
{
    int i,n,sum=0;
    printf("Number of terms:");
    scanf("%d",&n);
    printf("The odd numbers are: %d",i);

    for(i=1;i<=n*2-1;i+=2){
            printf(" %d",i);
    sum=sum+i;}
    printf("\ntheir sum: %d",sum);

    return 0;
}
